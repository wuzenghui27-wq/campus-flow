# 校招迹

本地优先的 Windows 校招投递工作台。投递记录、个人资料和简历路径只保存在当前电脑。

## 下载

- `校招迹-自动更新版-*-安装.exe`：推荐使用，安装后从 GitHub Releases 自动更新。
- `校招迹-*-portable.exe`：无需安装的便携版，需手动下载新版本替换。

## 发布新版本

更新 `package.json` 中的版本号，提交后推送同名标签，例如 `v1.0.1`。GitHub Actions 会自动测试、打包并发布。

## 招迹 v1.0.18

- 合并「简历」和「个人信息」为一个整篇编辑页面；不再拆分字段，没有PDF也可以填写、保存和复制全文。
- PDF优先读取文字层；Mac新增本机扫描识别，短文字页不再误触发OCR，读取失败会保留其他页并提示原因。
- 支持同一区域切换文字与PDF原版，编辑后可恢复提取原文。
- 取消、读取或保存失败不会覆盖已保存正文；切换页面保留未保存编辑。
- 个人信息全文、原始提取副本和PDF路径按账号保存在本机，不上传云端。投递同步保持现有行为；本版不包含另行开发的加密同步功能。

### 安装与更新

- Windows x64：下载 `campus-flow-setup-1.0.18.exe` 安装；已有安装版重启后检查更新，点击更新按钮下载并安装。
- Apple芯片Mac（macOS 12及以上）：下载 `campus-flow-1.0.18-mac-arm64.dmg`，退出旧版后拖到「应用程序」替换；Mac更新按钮打开下载页。
- Mac数据保存在 `~/Library/Application Support/招迹/`，替换app不会覆盖该目录。

Mac版本尚未配置Apple开发者签名和公证。如果系统拦截，请在「系统设置 → 隐私与安全性」核对来源后允许打开。本安装包不适用于Intel Mac。
